const nombre = 'Deadpool';
const real = 'Wade Winston';

const completo = nombre + ' ' + real;
const template = `${nombre} ${real}`;


console.log(completo);
console.log(template);
console.log(completo === template);


const html = 
`
    <h1>Hola</h1>
    <p>Putos</p>
`;

console.log(html);